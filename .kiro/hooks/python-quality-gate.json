{
  "version": "v1",
  "hooks": [
    {
      "name": "Python Quality Gate",
      "trigger": "PostFileSave",
      "matcher": "\\.py$",
      "action": {
        "type": "agent",
        "prompt": "Perform Python quality validation on the saved file:\n\n1. Check Python syntax validity\n2. Run basic linting (pylint or flake8 if available)\n3. Report any syntax errors or linting issues found\n4. Note which tools are available and which are missing\n\nIf tools are not installed, report availability rather than failing. Do NOT install dependencies automatically."
      }
    }
  ]
}